Usage meter for quotas and balances. Switch tone at thresholds.

```jsx
<ProgressBar value={68} />
<ProgressBar value={91} tone="warning" />
<ProgressBar value={12} max={100} tone="danger" height={6} />
```

- Green fill glows. Use warning ≥80%, danger ≥95% for consumption meters.
