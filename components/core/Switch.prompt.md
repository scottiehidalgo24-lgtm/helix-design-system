Binary on/off toggle for settings. Controlled — pass `checked` and `onChange`.

```jsx
<Switch checked={autoscale} onChange={setAutoscale} label="Autoscale" />
<Switch checked size="sm" />
```

- Glows neon green when on. Use for instant-apply settings, not form submission.
